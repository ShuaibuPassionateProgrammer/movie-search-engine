import React, { useEffect, useState } from "react";

const Button = () => {
    const [name, setName] = useState("Shuaibu");

    return (
        <div>
            <h1></h1>
        </div>
    );
};

export default Button;